public interface InterfaceAccount {
    boolean cashWithdraw(int withdrawAmount);
    boolean  cashDeposit(Double dipositeAmount);
     int accountNumberGenerate();
     int accountNumberGenerate(String nullValue);
}
